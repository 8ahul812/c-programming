#include<stdio.h>    // # also called preprocessor
#include<math.h>   // math.h is  library and it is stored in include   // math.h generally used in math types calcula tions
int main(){
int n;
printf("Enter the number :");
scanf("%d",&n);
int root = sqrt(n);
printf("The squre root is : %d",root );
}